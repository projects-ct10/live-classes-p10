apiVersion: v1
kind: Pod
metadata:
  name: nginx
  lables:
    app: nginx
  namespace: xyz
spec:
  containers:
  - name: nginx
    image: nginx:1.14.4
    ports:
    - containerPort: 80
  - name: frontend
    image: nginx:1.14.2
    ports:
    - containerPort: 80
    env:
     - name: COLOR
       value: blue

*.yaml 

====================

Namespace : A sechuled box inside your cluster. (2 clustomers - A,B)


kubectl --> command line for kubernetes ( kubectl apply -f abc.yaml)